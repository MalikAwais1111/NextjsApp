/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.dummyjson.com'], // Add the domain where the images are hosted
    },
  };
  
  export default nextConfig;
  